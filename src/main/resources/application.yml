spring:
  application:
    name: commit

  datasource:
    url: jdbc:postgresql://db.fuuciatooaawrnkupjms.supabase.co:5432/postgres?sslmode=require
    username: postgres
    password: bhavesh@Commit
    hikari:
      maximum-pool-size: 5
      minimum-idle: 2
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
      leak-detection-threshold: 60000

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: ${PORT:8080}

# Firebase configuration
firebase:
  credentials:
    path: firebase-service-account.json
    json: ${FIREBASE_CREDENTIALS_JSON:}
