spring:
  application:
    name: commit

  datasource:
    url: jdbc:postgresql://aws-1-ap-south-1.pooler.supabase.com:6543/postgres?sslmode=require&pgbouncer=true
    username: postgres.fuuciatooaawrnkupjms
    password: bhavesh@Commit
    hikari:
      maximum-pool-size: 3
      minimum-idle: 1
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
      leak-detection-threshold: 60000

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: ${PORT:8080}

# Firebase configuration
firebase:
  credentials:
    path: firebase-service-account.json
    json: ${FIREBASE_CREDENTIALS_JSON:}
